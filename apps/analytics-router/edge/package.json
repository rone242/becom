{
  "name": "@becom/analytics-edge",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare Worker edge layer — API-key auth, rate limiting, geo-enrichment, and forwarding for the analytics-router",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\" --fix"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.0"
  }
}
